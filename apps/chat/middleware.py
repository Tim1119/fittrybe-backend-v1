"""
JWT WebSocket authentication middleware.
Stub — full implementation added in Phase 2 (chat module).
"""


def JwtAuthMiddlewareStack(inner):
    """Stub middleware that passes through to the inner application."""
    return inner
